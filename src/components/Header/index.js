import React from 'react';
import Nav from '../Nav'

function Header(props) {
    return (
        <header>
            This is the header
            <Nav />
        </header>
    );
}

export default Header;